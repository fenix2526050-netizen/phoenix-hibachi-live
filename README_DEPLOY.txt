Phoenix Hibachi V2.2.6 — GitHub Upload Folder

Upload the CONTENTS of this folder to the GitHub Pages repository root.
Do not upload the parent 02_SUPABASE or 03_DOCS folders to GitHub.

Important:
1. Run 02_SUPABASE/01_FIX_BOOKING_SUBMIT_NOW.sql in Supabase first.
2. This build saves booking requests even if older optional payment columns are still absent.
3. Stripe, Gift Card, Phoenix Credit and points remain disabled until secure Edge Functions and keys are deployed.
4. No video file is stored in GitHub. The hero uses a poster until an R2 URL is published in Supabase video_assets.
5. Never put service-role, Stripe secret, webhook secret or Cloudflare API tokens in this folder.
